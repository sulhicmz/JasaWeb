{
  "name": "@jasaweb/config",
  "version": "1.0.0",
  "description": "Shared configuration for JasaWeb",
  "scripts": {
    "build": "echo 'No build step required'",
    "lint": "echo 'No lint step required'",
    "format": "prettier --write tsconfig/**/*.json eslint/**/*.js prettier/**/*.js"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}