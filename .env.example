# Environment Variables Example
# Copy this to .env for local development

# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@hostname/database?sslmode=require"

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET="your-secret-key-here"

# Midtrans Payment Gateway (CRITICAL for production)
# Get your keys from dashboard.midtrans.com
# 
# PRODUCTION SETUP:
# 1. Get production keys from Midtrans dashboard
# 2. Set MIDTRANS_IS_PRODUCTION="true"
# 3. Configure webhook endpoint in Midtrans dashboard: https://your-domain.com/api/webhooks/midtrans
# 4. Ensure SSL is enabled (required for production webhooks)
# 5. Test with small amounts before going live

# SANDBOX (for development/testing):
MIDTRANS_SERVER_KEY="SB-Mid-server-xxx"
MIDTRANS_CLIENT_KEY="SB-Mid-client-xxx"
MIDTRANS_IS_PRODUCTION="false"

# Production (DO NOT commit to version control):
# MIDTRANS_SERVER_KEY="Mid-server-PRODUCTION_KEY_HERE"
# MIDTRANS_CLIENT_KEY="Mid-client-PRODUCTION_KEY_HERE"
# MIDTRANS_IS_PRODUCTION="true"

# Cloudflare (automatically configured in Workers)
# CLOUDFLARE_API_TOKEN="your-cloudflare-api-token"
