queue_rules:
  - name: open pr
    merge_method: rebase
    update_method: rebase
    autoqueue: true

# Priority rules to ensure important PRs are processed quickly
priority_rules:
  - name: main-branch-high-priority
    priority: high
    conditions:
      - base = main
    allow_checks_interruption: false
